PR #59 — sentinel/fix-hardcoded-nvidia-key-15999701295688394101

Conflicts detected when attempting to merge this branch into `origin/main`:
- `api/ai-analyst.ts` (content conflict)
- `package.json` (auto-merge failed)

Suggested action for PR author:
- Rebase branch onto `origin/main` and resolve the conflicts in the listed files.
- Ensure `api/ai-analyst.ts` changes remain correct and that `package.json` version/lock changes are intentional.

Suggested reviewer checklist:
- Confirm the security fix still applies after resolution.
- Run CI/tests and verify no new vulnerabilities introduced.
